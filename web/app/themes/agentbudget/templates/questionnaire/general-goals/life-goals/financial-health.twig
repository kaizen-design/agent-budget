<form class="questionnaire-form h-100 py-5 d-none" data-id="{{ id }}" novalidate>
  <div class="container-fluid h-100">
    <div class="row h-100 align-items-center justify-content-center">
      <div class="col-xl-11 col-xxl-8 mt-auto">
        <small class="form-title d-block mb-3">GENERAL GOALS</small>
        <p class="lead text-dark fs-26 mb-4">Using the scale below, describe how important <strong class="fw-800">“financial health”</strong> is to your overall life goals.</p>
        <div class="btn-group d-flex mw-xxl-780" role="group" aria-label="Financial health button group">
          {% for i in range(1, 10) %}
            <input type="radio" class="btn-check" name="financial_health" id="financial-health-{{ loop.index }}" value="{{ loop.index }}" required autocomplete="off">
            <label class="btn btn-outline-primary" for="financial-health-{{ loop.index }}">{{ loop.index }}</label>
          {% endfor %}
        </div>
        <div class="button-group-labels d-flex justify-content-between mw-xxl-780 mt-2">
          <small>Not important</small>
          <small>Very important</small>
        </div>
        <div class="mt-5">
          <button type="submit" class="btn btn-primary btn-lg mw-90 me-2">OK</button>
          <small class="press-enter-btn text-dark fw-500">Press <strong class="fw-800">Enter ↵</strong></small>
        </div>
      </div>
      <div class="col-xl-11 col-xxl-8 mt-5 mt-lg-auto">
        {% include 'questionnaire/partials/pagination-progress.twig' %}
      </div>
    </div>
  </div>
</form>
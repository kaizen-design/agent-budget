<section id="recruiting-overrides" class="report-section py-5 py-xl-7">
  <div class="container">
    <h2 class="section-title mb-3">
      <small class="d-block mb-3">REVENUE SHARE</small>
      <div class="d-flex justify-content-between align-items-center">
        <span>Recruiting & Overrides</span>
        <ul class="report-actions-list list-inline mb-0 d-flex d-print-none">
          <li class="list-inline-item me-3">
            <a href="#" class="printLink" data-id="recruiting-overrides">Print <i class="fas fa-print"></i></a>
          </li>
          <li class="list-inline-item">
            <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
          </li>
        </ul>
      </div>
    </h2>
    <p class="lead mb-5">It was a humorously perilous business for both of us. For, before we proceed further, it must be said that the monkey-rope was fast at both ends; fast to Queequeg's broad canvas belt, and fast to my narrow leather one. <strong class="fw-800">So that for better</strong> or for worse, we two, for the time, were wedded; and should poor Queequeg si.</p>
    
    {% for level in report.recruiting %}
      <small class="table-label d-block mb-2">LEVEL {{ loop.index }}</small>
      <div class="row {{ not loop.last ? 'mb-4' }}">
        {% if level.agents %}
          <div class="col-sm-6 col-lg-3 mb-3 mb-lg-0">
            <div class="report-card rounded bg-white border-start border-8 border-success p-3">
              <span class="card-label d-block mb-2">AGENTS RECRUITS</span>
              <span class="card-value d-block">
                {{ level.agents }}
              </span>
            </div>
          </div>
        {% endif %}
        {% if level.transactions %}
          <div class="col-sm-6 col-lg-3 mb-3 mb-lg-0">
            <div class="report-card rounded bg-white border-start border-8 border-info p-3">
              <span class="card-label d-block mb-2">TRANSACTIONS/RECRUIT</span>
              <span class="card-value d-block">
                {{ level.transactions }}
              </span>
            </div>
          </div>
        {% endif %}
        {% if level.highest_sold_price %}
          <div class="col-sm-6 col-lg-3 mb-3 mb-lg-0">
            <div class="report-card rounded bg-white border-start border-8 border-warning p-3">
              <span class="card-label d-block mb-2">HIGHEST SOLD PRICE</span>
              <span class="card-value d-block">
                {{ '$' ~ fn('d_nice_number', level.highest_sold_price|replace({'$' : ''})) }}
              </span>
            </div>
          </div>
        {% endif %}
        {% if level.lowest_sold_price %}
          <div class="col-sm-6 col-lg-3 mb-3 mb-lg-0">
            <div class="report-card rounded bg-white border-start border-8 border-pink p-3">
              <span class="card-label d-block mb-2">LOWEST SOLD PRICE</span>
              <span class="card-value d-block">
                {{ '$' ~ fn('d_nice_number', level.lowest_sold_price|replace({'$' : ''})) }}
              </span>
            </div>
          </div>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</section>
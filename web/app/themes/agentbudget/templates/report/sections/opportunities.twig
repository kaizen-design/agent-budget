<section id="opportunities" class="report-section py-5 py-xl-7 bg-white">
  <div class="container">
    <h2 class="section-title mb-3">
      <small class="d-block mb-3">REVENUE SHARE</small>
      <div class="d-flex justify-content-between align-items-center">
        <span>Agent, Inc. Opportunities</span>
        <ul class="report-actions-list list-inline mb-0 d-flex d-print-none">
          <li class="list-inline-item me-3">
            <a href="#" class="printLink" data-id="opportunities">Print <i class="fas fa-print"></i></a>
          </li>
          <li class="list-inline-item">
            <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
          </li>
        </ul>
      </div>
    </h2>
    <p class="lead mb-5">It was a humorously perilous business for both of us. For, before we proceed further, it must be said that the monkey-rope was fast at both ends; fast to Queequeg's broad canvas belt, and fast to my narrow leather one. <strong class="fw-800">So that for better</strong> or for worse, we two, for the time, were wedded; and should poor Queequeg si.</p>
    {% for key, opportunity in report.opportunities %}
      {% if opportunity %}
        <div class="row align-items-center">
          <div class="col-lg-4 mb-3">
            <img class="img-fluid"
                 src="{{ theme_url }}/assets/images/{{ key|replace({'_' : '-'}) }}-logo.png"
                 alt="{{ key|replace({'_' : ' '})|capitalize }}" width="207">
          </div>
          <div class="col-sm-6 col-md-4 col-lg-2 mb-3">
            <div class="report-card rounded border-bottom border-8 border-secondary py-3 px-2 text-center">
              <span class="card-label d-block mb-2">INTRODUCTIONS</span>
              <span class="card-value d-block">
                {{ opportunity.introductions }}
              </span>
            </div>
          </div>
          <div class="col-sm-6 col-md-4 col-lg-2 mb-3">
            <div class="report-card rounded border-bottom border-8 border-info py-3 px-2 text-center">
              <span class="card-label d-block mb-2">SUCCESS RATE</span>
              <span class="card-value d-block">
                {{ opportunity.success_rate }}
              </span>
            </div>
          </div>
          <div class="col-sm-6 col-md-4 col-lg-2 mb-3">
            <div class="report-card rounded border-bottom border-8 border-pink py-3 px-2 text-center">
              <span class="card-label d-block mb-2">TRANSACTIONS</span>
              <span class="card-value d-block">
                {{ opportunity.transactions }}
              </span>
            </div>
          </div>
          <div class="col-sm-6 col-md-4 col-lg-2 mb-3">
            <div class="report-card rounded border-bottom border-8 border-primary py-3 px-2 text-center">
              <span class="card-label d-block mb-2">AVG COST</span>
              <span class="card-value d-block">
                {{ opportunity.avg_cost == '' ? '$0' : '$' ~ fn('d_nice_number', opportunity.avg_cost|replace({'$' : ''})) }}
              </span>
            </div>
          </div>
        </div>
      {% endif %}
    {% endfor %}
  </div>
</section>